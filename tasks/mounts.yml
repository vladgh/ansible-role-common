---
- name: Mount Points
  mount:
    path: "{{ item.path }}"
    src: "{{ item.src }}"
    opts: "{{ item.opts }}"
    state: "{{ item.state }}"
    fstype: "{{ item.fstype }}"
  when: mount_points is defined
  with_items: "{{ mount_points }}"
